{
	"extends": "astro/tsconfigs/strict",
	"include": [".astro/types.d.ts", "**/*"],
	"exclude": ["dist"],
	"compilerOptions": {
		"baseUrl": ".", // base directory to resolve non-relative imports
		"paths": {
			"@/*": ["src/*"] // @/... will map to <projectRoot>/src/...
		}
	}
}
